# ~/.tmux.conf
# 说明:
# - 目标是最小但稳定的共享 tmux 基线
# - 不引入插件管理器，避免新机器首启依赖过多
# - tmux 文档: https://man7.org/linux/man-pages/man1/tmux.1.html

# 使用支持 256 色的终端类型，避免颜色和终端能力识别异常。
set -g default-terminal "tmux-256color"
# 每个 pane 保留更多历史输出，便于回看日志。
set -g history-limit 100000
# 开启鼠标支持：可直接点选 pane、拖动分隔线、滚轮滚动历史。
set -g mouse on
# 窗口编号从 1 开始，而不是默认的 0。
set -g base-index 1
# pane 编号也从 1 开始，和窗口编号保持一致。
setw -g pane-base-index 1

# 恢复 tmux 默认前缀键 C-b，并移除旧的 C-a 前缀映射。
set -g prefix C-b
unbind C-a

# 重新加载配置时给出明确反馈。
bind r source-file ~/.tmux.conf \; display-message "tmux config reloaded"

# copy mode / 滚动历史 / 文本选择等“模式按键”使用 vi 风格，而不是 emacs 风格。
setw -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection     # Begin selection in copy mode.
bind-key -T copy-mode-vi 'C-v' send -X rectangle-toggle  # Begin selection in copy mode.
bind-key -T copy-mode-vi 'y' send -X copy-selection  

# Pane Controls
bind v split-window -v -c "#{pane_current_path}"
bind h split-window -h -c "#{pane_current_path}"
bind x kill-pane


# bind -n C-[ select-pane -L
# bind -n C-] select-pane -R
# bind -n C-= select-pane -U
# bind -n C-\' select-pane -D

bind -n C-] select-pane -t :.+
bind -n C-[ select-pane -t :.-

bind -n M-h resize-pane -L 5
bind -n M-j resize-pane -D 5
bind -n M-k resize-pane -U 5
bind -n M-l resize-pane -R 5
# Window navigation
# bind r command-prompt -I "#W" "rename-window -- '%%'"
bind c new-window -c "#{pane_current_path}"
bind k confirm-before -p "Kill window #W? (y/n)" kill-window

# General
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*:RGB"
set -g mouse on
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g history-limit 50000
set -g escape-time 0
set -g focus-events on
set -g set-clipboard on
set -g allow-passthrough on
setw -g aggressive-resize on
set -g detach-on-destroy off

# Status bar
set -g status-position top
set -g status-interval 5
set -g status-left-length 30
set -g status-right-length 50
set -g window-status-separator ""
set -gw automatic-rename on
set -gw automatic-rename-format '#{b:pane_current_path}'

# Theme
set -g status-style "bg=default,fg=default"
set -g status-left "#[fg=black,bg=blue,bold] #S #[bg=default] "
set -g status-right "#[fg=blue]#{?pane_in_mode,COPY ,}#{?client_prefix,PREFIX ,}#{?window_zoomed_flag,ZOOM ,}#[fg=brightblack]#h "
set -g window-status-format "#[fg=brightblack] #I:#W "
set -g window-status-current-format "#[fg=blue,bold] #I:#W "
set -g pane-border-style "fg=brightblack"
set -g pane-active-border-style "fg=blue"
set -g message-style "bg=default,fg=blue"
set -g message-command-style "bg=default,fg=blue"
set -g mode-style "bg=blue,fg=black"
setw -g clock-mode-colour blue